<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aipass.dao.MemberMapper">

    <select id="findByUsername" parameterType="String" resultType="com.aipass.dto.MemberDTO">
        SELECT id, username, password, name, email, phone, role, created_at
        FROM member
        WHERE username = #{username}
    </select>

    <select id="countByUsername" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM member WHERE username = #{username}
    </select>

    <insert id="insertMember" parameterType="com.aipass.dto.MemberDTO">
        INSERT INTO member (username, password, name, email, phone, role, created_at)
        VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{role}, NOW())
    </insert>

    <select id="findByNameAndEmail" resultType="com.aipass.dto.MemberDTO">
        SELECT id, username, name, email, created_at
        FROM member
        WHERE name = #{name} AND email = #{email}
    </select>

    <select id="findByUsernameAndEmail" resultType="com.aipass.dto.MemberDTO">
        SELECT id, username, name, email
        FROM member
        WHERE username = #{username} AND email = #{email}
    </select>

    <update id="updatePassword">
        UPDATE member SET password = #{password} WHERE username = #{username}
    </update>

    <update id="updateMember" parameterType="com.aipass.dto.MemberDTO">
        UPDATE member
        SET name = #{name}, email = #{email}, phone = #{phone}
        WHERE username = #{username}
    </update>

</mapper>
