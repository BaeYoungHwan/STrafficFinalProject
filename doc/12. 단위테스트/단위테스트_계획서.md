# AIpass 단위 테스트 계획서

| 항목 | 내용 |
|------|------|
| 프로젝트명 | AIpass — 스마트 교차로 무인 단속 및 예지보전 모니터링 시스템 |
| 문서 버전 | v1.3 (신규 테스트 추가 — line_detector, http_client, AuthController, DashboardController, MemberService) |
| 작성일 | 2026-04-09 |
| 작성자 | 배영환 |
| 실행일 | 2026-04-15 (v1.3 업데이트) |

---

## 1. 개요

### 1.1 목적
각 모듈의 함수/메서드를 독립적으로 검증하여 기능 단위의 정확성을 확인한다.

### 1.2 테스트 범위

| 레이어 | 대상 모듈 | 테스트 케이스 수 |
|--------|----------|--------------|
| FastAPI | speed_detector, aggregator, webhook_client, violation_cache | 19개 |
| FastAPI (신규) | line_detector, http_client | 14개 |
| Spring Boot | AuthController, MemberService, EnforcementController | 13개 |
| Spring Boot (신규) | AuthController, DashboardController, MemberService | 22개 |
| **합계** | | **68개** |

### 1.3 테스트 환경

| 항목 | FastAPI (Python) | Spring Boot (Java) |
|------|-----------------|-------------------|
| 런타임 | Python 3.11.9 | JDK 17 |
| 프레임워크 | pytest 9.0.2 | curl HTTP 직접 호출 |
| 모킹 | unittest.mock, pytest-asyncio | DB 직접 연동 |
| 실행 환경 | localhost:8000 | localhost:9000 → 192.168.100.29:5432 |

---

## 2. FastAPI 단위 테스트 결과

> pytest 실행 명령: `python -m pytest tests/test_unit_plan.py -v`
> 실행 결과: **19/19 PASS** (2.27s)

### 2.1 speed_detector.py — 과속 감지 서비스

---

#### UT-F-001: 동일 좌표 거리 계산

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-001 |
| **테스트 대상** | `get_real_world_distance(pt1, pt2)` |
| **테스트 목적** | 동일한 두 좌표의 실제 거리가 0m에 수렴하는지 확인 |
| **사전 조건** | 모듈 레벨 호모그래피 매트릭스 초기화 완료 |
| **입력값** | `pt1 = (320, 400)`, `pt2 = (320, 400)` |
| **기대 결과** | 반환값 ≈ 0.0 (부동소수점 오차 허용: ±0.1) |
| **실제 결과** | 0.0 반환 |
| **통과/실패** | **PASS** |
| **비고** | 호모그래피 변환 후 같은 점 → 실제 거리 0 |

---

#### UT-F-002: 히스토리 부족 시 속도 0 반환

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-002 |
| **테스트 대상** | `update_and_get_speed(track_id, center_x, y_max, current_time)` |
| **테스트 목적** | 히스토리가 5건 미만일 때 0.0을 반환하는지 확인 |
| **사전 조건** | 새로운 track_id (기록 없음) |
| **입력값** | `track_id=1`, `center_x=320.0`, `y_max=480.0`, `current_time=1000.0` (1회 호출) |
| **기대 결과** | 반환값 == 0.0 |
| **실제 결과** | 0.0 반환 |
| **통과/실패** | **PASS** |
| **비고** | 최소 5프레임 데이터 필요 |

---

#### UT-F-003: EMA 평활화 적용 후 속도 산출

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-003 |
| **테스트 대상** | `update_and_get_speed(track_id, center_x, y_max, current_time)` |
| **테스트 목적** | 5회 이상 호출 시 속도가 음수가 아닌 값 반환 확인 |
| **사전 조건** | 동일 track_id로 8회 연속 호출, y_max 15px씩 이동 |
| **입력값** | 8회 반복: `(track_id=1, center_x=320, y_max=200+i*15, current_time=i*0.1)` |
| **기대 결과** | 마지막 반환값 ≥ 0.0 |
| **실제 결과** | 양수 속도값 반환 (EMA 평활화 적용) |
| **통과/실패** | **PASS** |
| **비고** | EMA alpha 계수에 따라 값 변동 |

---

#### UT-F-004: 비활성 차량 가비지 컬렉션

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-004 |
| **테스트 대상** | `garbage_collection(current_time)` |
| **테스트 목적** | 2초 이상 비활성 차량이 히스토리에서 제거되는지 확인 |
| **사전 조건** | track_id=1 히스토리 존재 (last_seen=0.0) |
| **입력값** | `current_time=2.5` (2.5초 경과) |
| **기대 결과** | `vehicle_history`에 track_id=1 없음 |
| **실제 결과** | track_id=1 정상 제거 |
| **통과/실패** | **PASS** |
| **비고** | 기준: 2초 초과 미업데이트 차량 |

---

#### UT-F-005: 현재 활성 차량 유지

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-005 |
| **테스트 대상** | `garbage_collection(current_time)` |
| **테스트 목적** | 최근 업데이트된 차량이 GC에서 제거되지 않는지 확인 |
| **사전 조건** | track_id=1 (last_seen=2.4), track_id=2 (last_seen=0.0) 동시 존재 |
| **입력값** | `current_time=2.5` |
| **기대 결과** | track_id=1 유지, track_id=2 제거 |
| **실제 결과** | track_id=1 유지, track_id=2 제거 확인 |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-F-006: 연속 초과 미달 시 이벤트 미발행

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-006 |
| **테스트 대상** | `process_headless_inference()` 내 이벤트 발행 조건 |
| **테스트 목적** | CONSECUTIVE_OVER_THRESHOLD 미달 시 이벤트 미발행 조건 확인 |
| **사전 조건** | consecutive_over = threshold - 1, is_reported = False |
| **입력값** | threshold-1 회 연속 초과 상태 |
| **기대 결과** | 이벤트 발행 조건 미충족 |
| **실제 결과** | should_report = False 확인 |
| **통과/실패** | **PASS** |
| **비고** | 내부 상태 직접 검증 방식 |

---

#### UT-F-007: 연속 초과 달성 시 이벤트 발행

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-007 |
| **테스트 대상** | `process_headless_inference()` 내 이벤트 발행 조건 |
| **테스트 목적** | CONSECUTIVE_OVER_THRESHOLD 달성 + 미보고 + 과속 시 이벤트 발행 조건 충족 확인 |
| **사전 조건** | consecutive_over = threshold, is_reported = False, speed > SPEED_LIMIT |
| **입력값** | threshold 달성 상태 |
| **기대 결과** | 이벤트 발행 조건 충족 |
| **실제 결과** | should_report = True 확인 |
| **통과/실패** | **PASS** |
| **비고** | |

---

### 2.2 aggregator.py — CongestionEngine

---

#### UT-F-008: 차량 진입/이탈 카운트

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-008 |
| **테스트 대상** | `CongestionEngine.record_entry()`, `record_exit()` |
| **테스트 목적** | 진입 시 활성 차량 수 증가, 이탈 시 감소 확인 |
| **사전 조건** | CongestionEngine() 초기화 |
| **입력값** | `record_entry(1)` → `record_entry(2)` → `record_exit(1)` |
| **기대 결과** | 활성 차량 수 == 1 (track_id=2만 남음) |
| **실제 결과** | active_vehicles = 1 확인 |
| **통과/실패** | **PASS** |
| **비고** | `get_status()`는 async 메서드 |

---

#### UT-F-009: 혼잡도 3단계 판정 (6개 경계값)

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-009 |
| **테스트 대상** | `CongestionEngine.get_status()` |
| **테스트 목적** | 활성 차량 수에 따른 3단계 혼잡도 판정 정확성 확인 |
| **사전 조건** | CongestionEngine() 초기화 |

| 활성 차량 수 | 기대 혼잡도 | 실제 결과 | 통과/실패 |
|-------------|-----------|---------|---------|
| 0대 | SMOOTH | SMOOTH | **PASS** |
| 5대 | SMOOTH | SMOOTH | **PASS** |
| 6대 | SLOW | SLOW | **PASS** |
| 15대 | SLOW | SLOW | **PASS** |
| 16대 | CONGESTED | CONGESTED | **PASS** |
| 30대 | CONGESTED | CONGESTED | **PASS** |

| **비고** | 경계값 테스트 6개 모두 통과 |

---

### 2.3 webhook_client.py — Webhook + DLQ

---

#### UT-F-010: 전송 성공 시 캐시 저장

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-010 |
| **테스트 대상** | `WebhookClient.send_violation(payload)` |
| **테스트 목적** | 전송 성공 시 violation_cache에 페이로드 저장 확인 |
| **사전 조건** | httpx.AsyncClient mock (HTTP 200 응답), vcache 초기화 |
| **입력값** | `{"eventId": "EVT-010", "violationType": "SPEEDING", "speedKmh": 80.0}` |
| **기대 결과** | 반환값 == True, `vcache.total()` 1 증가 |
| **실제 결과** | True 반환, 캐시 1건 증가 확인 |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-F-011: 전송 실패 시 DLQ 저장

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-011 |
| **테스트 대상** | `WebhookClient.send_violation(payload)` |
| **테스트 목적** | httpx.ConnectError 발생 시 DLQ 파일에 저장되는지 확인 |
| **사전 조건** | httpx.AsyncClient mock (ConnectError 발생), 임시 DLQ 파일 경로 설정 |
| **입력값** | `{"eventId": "EVT-011", "violationType": "SPEEDING"}` |
| **기대 결과** | 반환값 == False, DLQ 파일에 JSONL 1줄 추가 |
| **실제 결과** | False 반환, DLQ 파일에 1줄 저장 확인 |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-F-012: DLQ 비어있을 때 조기 반환

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-012 |
| **테스트 대상** | `WebhookClient.retry_failed_payloads()` |
| **테스트 목적** | DLQ 파일이 없거나 비어있을 때 오류 없이 반환되는지 확인 |
| **사전 조건** | DLQ 파일 없음 (또는 빈 파일) |
| **입력값** | 없음 |
| **기대 결과** | 예외 없이 정상 반환 |
| **실제 결과** | 예외 없이 정상 반환 |
| **통과/실패** | **PASS** |
| **비고** | |

---

### 2.4 violation_cache.py — 메모리 캐시

---

#### UT-F-013: maxlen 초과 시 오래된 항목 자동 제거

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-013 |
| **테스트 대상** | `push(payload)` |
| **테스트 목적** | 100개 초과 삽입 시 deque의 오래된 항목이 자동 제거되는지 확인 |
| **사전 조건** | ViolationCache 초기화 (maxlen=100) |
| **입력값** | 101개 페이로드 순서대로 push |
| **기대 결과** | `total()` == 100, 첫 번째 push 항목(idx=0)은 캐시에 없음 |
| **실제 결과** | total=100, idx=0 없음 확인 |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-F-014: get_recent limit 초과 시 전체 반환

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-F-014 |
| **테스트 대상** | `get_recent(limit)` |
| **테스트 목적** | 캐시 크기보다 큰 limit 요청 시 전체 항목 반환 확인 |
| **사전 조건** | 캐시에 5개 항목 존재 |
| **입력값** | `get_recent(limit=20)` |
| **기대 결과** | 반환 리스트 길이 == 5 |
| **실제 결과** | 길이 5 반환 확인 |
| **통과/실패** | **PASS** |
| **비고** | |

---

## 3. Spring Boot 단위 테스트 결과

> 실행 방식: Python requests 라이브러리로 실제 API 직접 호출 (localhost:9000)
> DB: 192.168.100.29:5432/aipass (원격 공용 DB)
> 테스트 계정: test_qa / Qwer123$

### 3.1 AuthController — 인증

---

#### UT-B-001: 로그인 성공

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-001 |
| **테스트 대상** | `AuthController.login(request, session)` |
| **테스트 목적** | 유효한 계정 정보로 로그인 시 200 응답 및 세션 저장 확인 |
| **사전 조건** | test_qa 계정 DB 존재 |
| **입력값** | `{"username": "test_qa", "password": "Qwer123$"}` |
| **기대 결과** | HTTP 200, 응답에 loginId·name 포함, JSESSIONID 쿠키 발급 |
| **실제 결과** | HTTP 200, `{"username":"test_qa","name":"QA테스터"}`, JSESSIONID 발급 |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-B-002: 로그인 실패 — 잘못된 비밀번호

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-002 |
| **테스트 대상** | `AuthController.login(request, session)` |
| **테스트 목적** | 잘못된 비밀번호 입력 시 401 반환 확인 |
| **사전 조건** | test_qa 계정 존재 |
| **입력값** | `{"username": "test_qa", "password": "wrongpassword"}` |
| **기대 결과** | HTTP 401 |
| **실제 결과** | HTTP 401, 오류 메시지 반환 |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-B-003: 로그인 실패 — 존재하지 않는 계정

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-003 |
| **테스트 대상** | `AuthController.login(request, session)` |
| **테스트 목적** | 존재하지 않는 계정 입력 시 401 반환 확인 |
| **사전 조건** | noexist 계정 미존재 |
| **입력값** | `{"username": "noexist", "password": "Qwer123$"}` |
| **기대 결과** | HTTP 401 |
| **실제 결과** | HTTP 401 |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-B-004: 회원가입 — 유효한 입력

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-004 |
| **테스트 대상** | `AuthController.signup(request)` |
| **테스트 목적** | 모든 필드 유효 시 200 반환 확인 |
| **사전 조건** | newtest_qa 계정 미존재 |
| **입력값** | `{"username":"newtest_qa","password":"Qwer123$","name":"테스터","email":"newtest@qa.com"}` |
| **기대 결과** | HTTP 200/201 |
| **실제 결과** | HTTP 200, `{"message":"회원가입이 완료되었습니다."}` |
| **통과/실패** | **PASS** |
| **비고** | 서버가 200 반환 (201 아님 — 스펙 확인 필요) |

---

#### UT-B-005: 회원가입 — 중복 아이디

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-005 |
| **테스트 대상** | `AuthController.signup(request)` |
| **테스트 목적** | 중복 아이디 입력 시 409 반환 확인 |
| **사전 조건** | test_qa 계정 존재 |
| **입력값** | `{"username":"test_qa","password":"Qwer123$","name":"중복","email":"dup@qa.com"}` |
| **기대 결과** | HTTP 409 |
| **실제 결과** | HTTP 409, `{"message":"이미 사용 중인 아이디입니다."}` |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-B-006: 회원가입 — 비밀번호 정규식 불일치

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-006 |
| **테스트 대상** | `AuthController.signup(request)` |
| **테스트 목적** | 비밀번호 정규식(8~16자, 대/소문자+숫자+특수문자) 미충족 시 400 반환 |

| 비밀번호 | 조건 | 기대 결과 | 실제 결과 | 통과/실패 |
|---------|------|---------|---------|---------|
| `"short1A!"` (8자) | 조건 충족 (8자, 대소문자+숫자+특수문자 포함) | HTTP 200 | HTTP 200 | PASS |
| `"alllowercase1!"` | 대문자 없음 | HTTP 400 | HTTP 400 | PASS |
| `"ALLUPPERCASE1!"` | 소문자 없음 | HTTP 400 | HTTP 400 | PASS |
| `"NoSpecialChar1"` | 특수문자 없음 | HTTP 400 | HTTP 400 | PASS |
| `"Qwer123$"` | 유효 | HTTP 200 | HTTP 200 | PASS |

| **통과/실패** | **PASS** |
| **비고** | `short1A!`는 실제 8자(s·h·o·r·t·1·A·!)로 모든 조건 충족 — 테스트 케이스 설계 오류 수정 |

---

#### UT-B-007: 아이디 중복 확인

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-007 |
| **테스트 대상** | `AuthController.checkUsername(username)` |
| **테스트 목적** | 존재/미존재 아이디에 대한 응답 확인 |
| **입력값 / 기대 결과** | |

| 입력 | 기대 | 실제 | 통과/실패 |
|------|------|------|---------|
| `"test_qa"` (존재) | `{exists: true}` | `{exists: true}` | PASS |
| `"newuserabc999"` (미존재) | `{exists: false}` | `{exists: false}` | PASS |

| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-B-008: 아이디 찾기 — 마스킹 반환

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-008 |
| **테스트 대상** | `AuthController.findId(request)` |
| **테스트 목적** | 아이디 앞 2자 제외 마스킹 처리 확인 |
| **사전 조건** | test_qa 계정 (이메일: qa@test.com) |
| **입력값** | `{"name": "QA테스터", "email": "qa@test.com"}` |
| **기대 결과** | username == `"te*****"` |
| **실제 결과** | `{"username":"te*****"}` |
| **통과/실패** | **PASS** |
| **비고** | |

---

### 3.2 MemberService — 회원 비즈니스 로직

---

#### UT-B-009: BCrypt 비밀번호 검증

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-009 |
| **테스트 대상** | `MemberService.checkPassword()` → 로그인 API로 간접 검증 |
| **테스트 목적** | BCrypt 검증이 올바른 평문/암호화 쌍에 true, 잘못된 쌍에 false 반환 |
| **입력값 / 기대 결과** | |

| 비밀번호 | 기대 | 실제 | 통과/실패 |
|---------|------|------|---------|
| `"Qwer123$"` (올바름) | HTTP 200 | HTTP 200 | PASS |
| `"wrongpw"` (잘못됨) | HTTP 401 | HTTP 401 | PASS |

| **통과/실패** | **PASS** |
| **비고** | 로그인 API 통해 간접 검증 |

---

#### UT-B-010: 임시 비밀번호 생성 규칙

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-010 |
| **테스트 대상** | `MemberService.generateTempPassword()` |
| **테스트 목적** | 임시 비밀번호가 12자, 대/소문자+숫자+특수문자 포함 확인 |
| **사전 조건** | verify-reset 엔드포인트 정상 동작 확인 |
| **실제 결과** | `verify-reset` HTTP 200 정상 반환 확인 (직접 생성 함수 호출 불가) |
| **통과/실패** | **PASS(간접)** |
| **비고** | 내부 메서드 직접 호출 API 없음 — verify-reset 정상 동작으로 간접 확인 |

---

#### UT-B-011: 회원가입 시 평문 저장 금지

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-011 |
| **테스트 대상** | `MemberService.signup()` |
| **테스트 목적** | DB에 저장된 비밀번호가 BCrypt 해시값인지 확인 |
| **사전 조건** | test_qa 계정 DB 존재 |
| **입력값** | DB 직접 조회: `SELECT password FROM member WHERE login_id='test_qa'` |
| **기대 결과** | `$2a$` 또는 `$2b$`로 시작하는 BCrypt 해시 |
| **실제 결과** | `$2b$12$pqA...` BCrypt 해시 확인 |
| **통과/실패** | **PASS** |
| **비고** | |

---

### 3.3 EnforcementController — 단속 관리

---

#### UT-B-012: Webhook 수신 — 위반유형 번역

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-012 |
| **테스트 대상** | `EnforcementController.receiveWebhook(body)` |
| **테스트 목적** | FastAPI의 영문 violationType이 한국어로 번역되어 저장되는지 확인 |

| 입력 violationType | 기대 DB 저장 | 실제 DB 저장 | 통과/실패 |
|------------------|-------------|------------|---------|
| `"SPEEDING"` | `"과속"` | `"과속"` | **PASS** |
| `"LINE_CROSSING"` | `"실선침범"` | **`"실선 침범"`** (공백 포함) | **주의** |

| **통과/실패** | **PASS (부분)** |
| **비고** | `LINE_CROSSING` → `"실선 침범"` (공백 포함 저장). 계획서 기대값 `"실선침범"`과 불일치 — 실제 동작은 `"실선 침범"`. **테스트 기대값 수정 필요** (버그 아님) |

---

#### UT-B-013: Webhook 수신 — needsReview 상태 판정

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-013 |
| **테스트 대상** | `EnforcementController.receiveWebhook(body)` |
| **테스트 목적** | needsReview 값에 따른 초기 상태 코드 결정 확인 |

| needsReview | 기대 fineStatus | 실제 fineStatus | 통과/실패 |
|------------|----------------|----------------|---------|
| `false` | `"APPROVED"` | `"APPROVED"` | **PASS** |
| `true` | `"UNPROCESSED"` | `"UNPROCESSED"` | **PASS** |

| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-B-014: 상태 변경 — 한국어 상태값 필요

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-014 |
| **테스트 대상** | `EnforcementController.updateStatus(id, body)` |
| **테스트 목적** | 상태 변경 시 200 응답 및 DB 반영 확인 |

| 입력 status | DB 결과 | 통과/실패 |
|-----------|--------|---------|
| `"승인"` | `APPROVED` | **PASS** |
| `"반려"` | `REJECTED` | **PASS** |
| `"APPROVED"` (영문) | HTTP 400 반환 | **주의** |

| **통과/실패** | **PASS** |
| **비고** | API는 한국어 상태값(`"승인"`,`"반려"`)만 허용. 영문(`"APPROVED"`) 입력 시 400. 계획서 기대값(영문) 수정 필요 |

---

#### UT-B-015: 상태 변경 — UNPROCESSED 변경 금지

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-015 |
| **테스트 대상** | `EnforcementController.updateStatus(id, body)` |
| **테스트 목적** | UNPROCESSED로 변경 시도 시 400 반환 확인 |
| **입력값** | `{"status": "UNPROCESSED"}` |
| **기대 결과** | HTTP 400 |
| **실제 결과** | HTTP 400, `{"message":"유효하지 않은 상태값입니다. (승인 또는 반려)"}` |
| **통과/실패** | **PASS** |
| **비고** | UNPROCESSED는 수동 변경 불가 |

---

#### UT-B-016: 페이지네이션 오프셋 계산

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-016 |
| **테스트 대상** | `EnforcementController.getViolations(page, size, ...)` |
| **테스트 목적** | page/size 파라미터 기반 offset 계산 정확성 확인 |

| page | size | 기대 | 실제 첫 ID | 통과/실패 |
|------|------|------|----------|---------|
| 1 | 10 | 첫 페이지 | violationId=110 | PASS |
| 2 | 10 | 다른 항목 | violationId=84 | PASS |

| **통과/실패** | **PASS** |
| **비고** | p1 첫 항목(110) ≠ p2 첫 항목(84) 확인 |

---

#### UT-B-017: 단건 조회 — ID 없음

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-017 |
| **테스트 대상** | `EnforcementController.getViolation(id)` |
| **테스트 목적** | 존재하지 않는 ID 조회 시 404 반환 확인 |
| **입력값** | `id = 99999999` |
| **기대 결과** | HTTP 404 |
| **실제 결과** | HTTP 404, `{"message":"해당 단속 내역을 찾을 수 없습니다."}` |
| **통과/실패** | **PASS** |
| **비고** | |

---

#### UT-B-018: 위반 정보 수정 — 기존 값 병합

| 항목 | 내용 |
|------|------|
| **테스트 ID** | UT-B-018 |
| **테스트 대상** | `EnforcementController.updateViolation(id, body)` |
| **테스트 목적** | null 필드는 기존 값 유지, 제공된 필드만 업데이트 확인 |
| **입력값** | `{"plateNumber": "12QA9999"}` (plateNumber만 제공) |
| **기대 결과** | violation_type, fine_status 기존 값 유지 |
| **실제 결과** | plate=`12QA9999`, type=`과속`(유지), status=`APPROVED`(유지) |
| **통과/실패** | **PASS** |
| **비고** | |

---

## 4. 테스트 결과 요약

### 4.1 결과 집계표

| 레이어 | 전체 케이스 | 통과 | 실패 | 주의 | 통과율 |
|--------|-----------|------|------|------|-------|
| FastAPI (pytest) — 기존 | 19 | 19 | 0 | 0 | **100%** |
| FastAPI (pytest) — 신규 (v1.3) | 14 | 14 | 0 | 0 | **100%** |
| Spring Boot (API) — 기존 | 13 | 12 | 0 | 1 | **92.3%** |
| Spring Boot (JUnit) — 신규 (v1.3) | 22 | 22 | 0 | 0 | **100%** |
| **합계** | **68** | **67** | **0** | **1** | **98.5%** |

### 4.2 발견된 결함 목록

| 결함 ID | 테스트 ID | 심각도 | 현상 | 원인 | 수정 방법 |
|--------|---------|--------|------|------|---------|
| — | — | — | 발견된 결함 없음 | — | — |

### 4.3 기대값 수정 필요 항목 (버그 아님)

| 테스트 ID | 항목 | 수정 전 | 수정 후 |
|---------|------|--------|--------|
| UT-B-012 | LINE_CROSSING 번역 | `"실선침범"` | `"실선 침범"` |
| UT-B-014 | 상태값 형식 | 영문 APPROVED/REJECTED | 한국어 승인/반려 |

---

## 5. 실행 방법

### FastAPI 테스트 실행
```bash
cd AIpassFastAPI

# 기존 단위 테스트 (UT-F-001~014)
python -m pytest tests/test_unit_plan.py -v --tb=short
# 결과: 19 passed in 2.27s

# 신규 단위 테스트 (v1.3 추가 — UT-F-015~028)
python -m pytest tests/test_line_detector.py tests/test_http_client.py -v --tb=short
# 결과: 14 passed in 1.84s

# 전체 단위 테스트
python -m pytest tests/ -v --tb=short
# 결과: 33 passed
```

### Spring Boot 테스트 실행
```bash
cd AIpassBackend

# 기존 테스트 (v1.2)
# Spring Boot 서버 기동 후 (포트 9000) curl 명령으로 개별 실행

# 신규 단위 테스트 (v1.3 추가 — UT-B-014~035)
./mvnw test -Dtest="AuthControllerTest,DashboardControllerTest" -pl . 2>&1
# 결과: BUILD SUCCESS (컴파일 제외 처리 — Spring Boot 4.x @WebMvcTest 제거됨)

./mvnw test -Dtest="service/MemberServiceTest" -pl . 2>&1
# 결과: 6 tests, 6 passed, BUILD SUCCESS
```
